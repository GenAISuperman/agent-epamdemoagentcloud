apiVersion: v1
kind: Service
metadata:
  name: "epamdemoagentcloud"
  namespace: "agents-devteam"
spec:
  selector:
    app: "epamdemoagentcloud"
  ports:
    - port: 80
      targetPort: 8000
  type: ClusterIP
