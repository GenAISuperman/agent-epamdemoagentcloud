agent:
  id: "epamdemoagentcloud"
  team: "devteam"
  runtime: aks
  interaction_model: "standalone"
  guardrail_profile: "strict"

identity:
  sp_client_id: "24a7c2d5-7070-423c-a478-777816de83bf"
  keyvault_url: "https://kv-shop-poc.vault.azure.net/"

tools: []

evaluation:
  suite: default-strict
  ci_gate: true
  pass_threshold: 0.95
  datasets:
    - id: golden-dataset
      version: v1.0
      source: repo
      path: evals/datasets/golden-dataset.json

output_policy:
  violation_threshold: 6
  window_minutes: 2
  action: block
  notify_webhook: 

prompts:
  active_version: v1.0
  source: prompts/system.txt
